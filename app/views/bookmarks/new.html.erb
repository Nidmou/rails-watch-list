<h1>Bookmarks#new</h1>
<p>Find me in app/views/bookmarks/new.html.erb</p>



<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, placeholder: "So what do you think about the movie" %>
  <%= f.association :movie, collection: Movie.all.order(:title), prompt: "Select a movie", input_html: { data: { controller: "slect2" } } %>

  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
<%=link_to "back to lits", lists_path%>
